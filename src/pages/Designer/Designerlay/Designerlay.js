import React from 'react'

function Designerlay(props) {
    const {onCha} =props
    return (
        <div onClick={()=>onCha()}>
            {/* designerlay */}
        </div>
    )
}

export default Designerlay
